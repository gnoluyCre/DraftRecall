一旦我所属的文件夹有所变化请更新我。
`supabase` 存放生产数据库架构和云同步边界说明。
本地演示用 localStorage，部署时执行 `schema.sql` 并接入 Supabase 环境变量。

- `schema.sql`: Cards、Question Items、Alarms、Records、Warehouse、AI Settings、Notification Settings、Push Subscriptions 表结构和索引。
