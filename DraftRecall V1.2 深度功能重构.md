
### **## 需求：卡片操作入口重构与功能扩展**

**1. 交互形式变更**

- **入口重构**：把当前设置突变占用的位置引入 **Kebab Menu (垂直三点图标)**。点击后触发一个**浮动菜单 (Popover Menu)**
    
- **视觉风格**：菜单需保持全站统一的高级感视觉规范（如 0.5px 极细边框、单色调、毛玻璃背景效果）。
    

**2. 菜单功能项说明**

- **设置 (Settings)**：
    
    - 点击后打开现有的卡片深度设置弹窗，供用户调整质询模式、闹钟时间或自定义问题。
        
- **重命名 (Rename)**：
    
    - 允许用户修改卡片的当前标题。修改后需立即更新卡片实体并同步至持久化存储中。
        
- **设定等级 (Priority)**：
    
    - 提供 **P0 (核心资产)**、**P1 (标准)**、**P2 (辅助)** 三个选项。
        
    - **联动逻辑**：用户更改等级后，Dashboard 应实时触发重新排序。P0 卡片需平滑地移动至核心置顶区域，P1/P2 按到期状态排列。
        
- **删除 (Delete)**：
    
    - 彻底移除该卡片及其关联的所有复习记录和闹钟配置。
        
    - **安全机制**：点击删除必须触发二次确认弹窗，以防误删正在复习阶段的知识资产。
- 查看题目（需求在后文）
        

**3. 交互细节要求**

- **反馈动效**：菜单的弹出与收起需使用平滑的过渡动画。
    
- **危险项警示**：菜单中的“删除”选项需使用醒目的颜色（如红色）以区别于普通管理操作。



### **## 需求：卡片设置页“提示词”模块 UI 深度优化**

**1. 布局重排与标签化 

- **位置交换**：将“打分”与“解析”提示词框的位置进行上下对调。
    
- **上方输入框**：
    
    - **标签**：命名为“**回答打分提示词**”。

- **下方输入框**：
    
    - **标签**：命名为“**文档解析提示词**”。

**2. 增强交互细节 (Enhanced UX)**

- **辅助说明 (Tooltips)**：在每个标签右侧增加一个微缩的“问号”图标。
    
    - **打分提示词提示**：展示“定义 AI 评价你回答的严苛程度及反馈角度”。
        
    - **解析提示词提示**：展示“决定 AI 从原文中提取知识点和生成问题的逻辑”。
        
- **视觉一致性**：
    
    - 保持全站 0.5px 极细边框和单色调极简风格。
        
    - 两个提示词区域之间增加适度的 **Margin-bottom**，让“重新解析问题”按钮具备更好的视觉呼吸感，避免界面拥挤。

**3. 布局重排与标签化 


### ## 需求：基于“动态知识要点”的解析与判分体系重构

#### **1. 核心逻辑变革：从“全文比对”到“要点映射”**

- **逻辑转向**：为了大幅削减复习阶段的 Token 成本并提升判分准度，系统将放弃在每次质询时向 AI 发送整篇原文的方案。
    
- **动态要点提取**：在录入阶段，AI 在生成问题时，需根据问题本身的知识密度，自主决定并提取出该问题对应的“核心得分要点”（Key Points）。这个需要你编写提示词
    
- **去固定化指令**：明确要求 AI 不要将要点数量固定在某个区间（如 3-5 个）。对于逻辑简单的内容，可以仅返回 1 个核心要点；对于复杂机制，应列出多个必要逻辑点。
    
#### **2. 判分引擎重构

- **判分输入优化**：调用 `/api/ai/grade` 时，**停止发送 `document`（原文）**，应该发送此问题关联的 key_points
    
- **动态判分准则**：判分 AI 需理解这组动态要点的逻辑权重。只要用户的回答在语义上覆盖了这些要点，即使措辞完全不同，也应判定为通过。
    
- **反馈增强**：AI 的评分反馈（Feedback）需结合这组动态要点，精准指出用户命中了哪些核心逻辑，漏掉了哪些细节。

### **## 需求补充：卡片问题与要点的“精细化编辑”**

**1. 交互与 UI 实现 (Editable List)**

- **列表化编辑**：在卡片设置页的“问题管理”区域，将原有的纯文本列表升级为“**问答对编辑组**”。
    
- **双输入框设计**：针对每一个生成的（或自定义的）问题，提供两个并列的输入框：
    
    - **问题输入框**：允许用户修改 AI 生成的问题描述，使其更符合个人的提问习惯。
        
    - **要点输入框**：允许用户修改该问题对应的“参考要点”。

**2. 业务逻辑增强 (Business Logic)**

- **即时同步**：用户修改任意一个输入框的内容后，系统需实时更新数据库
    
- **判分联动**：一旦用户修改了“参考要点”，下一次质询该问题时，`/api/ai/grade` 必须立即采用用户修改后的新要点作为判分基准。
    
- **自定义优先**：手动修改的内容应具有最高优先级。除非用户点击“重新解析问题”，否则 AI 不应覆盖用户手动编辑过的问题和要点。
    

**3. 视觉规范补充**

- **结构化布局**：为了避免 30 个问题导致页面过长，建议每个“问答对”采用紧凑的垂直堆叠或左右分栏布局，并使用 0.5px 的淡淡边框进行视觉隔离。
    
- **空值处理**：如果用户删除了某个问题的所有要点，系统应给出微弱的视觉提示（如红边），提醒该问题可能因缺少判分依据而导致 AI 无法准确打分。


### **## 需求：系统设置页面的功能升级**

#### **1. 高级主题引擎 (Advanced Professional Themes)**

- **主题设置**：提供一个主题选择器，支持以下 6 种模式：
    
    - **跟随系统 (Sync System)**：自动检测并切换深色/浅色。
        
    - **深色模式 (Deep Dark)**：极致黑与深灰的层次感，适合沉浸式学习。
        
    - **浅色模式 (Pure White)**：高亮白与米白的柔和过渡，视觉清爽。
        
    - **森林绿 (Forest Sage)**：采用低饱和度的莫兰迪绿，营造护眼且专业的学习氛围。
        
    - **复古粉 (Dusty Rose)**：灰粉配色，优雅而不甜腻，极具高级感。
        
    - **暮光紫 (Misty Lavender)**：脱色的雾面紫，平衡了知性与神秘感。
        
- **视觉要求**：
    
    - **禁忌**：禁止使用高饱和度、刺眼的纯色或简单的单色填充。
        
    - **色彩逻辑**：每种主题需包含一组精心调配的变量（主色、背景色、边框色、文字色），确保在任何颜色下，**0.5px 的边框**和**毛玻璃效果**（Glassmorphism）都能完美呈现。
        
    - **平滑过渡**：切换主题时，页面所有组件需通过 **Framer Motion** 实现无缝的色彩渐变过渡。


### ## 需求：原子化质询与“看要点”逻辑重构

#### **1. 答题流：从“打包提交”到“单题即时反馈”**

- **独立提交**：质询页面的 3 道随机题目不再共用一个提交按钮。每道题下方均配备独立的“**提交答案**”按钮。
    
- **即时判分**：用户完成单题回答并提交后，系统立即调用 `/api/ai/grade`（基于该题的动态要点）并原地展示该题的分数、反馈及要点命中情况。
    
- **清除与重试**：每道题提供“清除回答”按钮。在最终结算前，用户可以根据 AI 反馈修改回答并再次提交判分。
    

#### **2. 功能重构：“看要点”替代“看答案”**

- **名称变更**：全站将“直接看答案”统一更名为“**查看提示要点**”或“**获取提示”。
    
- **文案重写**：
    
    - _原逻辑_：点击即代表“我不会”，系统直接判定失败。
        
    - _新逻辑_：文案应提示“点击查看知识核心要素，辅助完善你的解释”。

**开关逻辑**：

- **首次点击**：展开并展示该题目关联的所有动态“参考要点”。
    
- **再次点击**：收起并隐藏已展示的要点。
    
- **重复性**：该功能支持在单题回答过程中**反复点击切换**。
- 
- **流程解耦**：
    
    - **无惩罚查看**：用户点击“看要点”后，系统**不触发**任何阶段回退或失败判定。
        
    - **后续操作**：查看要点后，输入框保持激活状态。用户可以参考要点重新组织语言进行回答，并正常提交 AI 判分。
        

#### **3. 最终结算与阶段推进 (Session Finalization)**

由于题目被拆开，卡片的阶段推进（Stage Update）逻辑需进行如下适配：

- **手动/自动结算**：当 3 道题均获得 AI 评分后，页面底部出现“**完成本次质询**”按钮。
    
- **综合判定准则**：
    
    - **全通判定**：系统检查这 3 道题的最高得分记录。只有当**每一道题**的最终得分均 > 60 分时，该卡片执行 `stage + 1`。
        
    - **失败判定**：若存在任何一题最终未达到 60 分（无论是否看过要点），点击结算时执行 `stage - 1`。

#### **4. 交互细节要求**
- **视觉反馈**：
    
    - 评分：显示具体分值，并根据分数高低显示不同颜色（如 > 60 绿，< 60 红）



### ## 需求：通知系统“双级开关”重构 (V1.1)

#### **1. 首页系统级总开关 (Global Dashboard Toggle)**

- **位置**：Dashboard（首页）顶层导航栏显著位置，通常位于“仓库统计”图标旁。
    
- **视觉逻辑**：
    
    - 使用标准的 **Bell（铃铛）** 图标。
        
    - **开启 (ON)**：铃铛图标呈现为**填充色 (Filled)**。代表全局推送功能已就绪。
        
    - **关闭 (OFF)**：铃铛图标呈现为**线框色 (Outline)**。代表系统进入全局静音模式，阻断所有推送。
        
- **交互反馈**：点击时通过 `Framer Motion` 产生轻微的 **Spring（弹性）** 缩放动效。状态实时同步至 `DraftRecallState` 并持久化到本地。
    

#### **2. 卡片级独立开关 (Per-Card Individual Toggle)**

- **位置**：固定在每张卡片的**右上角**，紧邻 Kebab Menu。
    
- **显示策略**：**始终可见 (Always Visible)**。即便用户没有将鼠标悬停在卡片上，图标也保持显示，方便用户一眼扫视全表。
    
- **视觉表现**：
    
    - **启用 (Active)**：显示为小尺寸的填充铃铛或高亮 Toggle。
        
    - **禁用 (Muted)**：显示为带有斜杠的铃铛或置灰图标。
        
- **覆盖逻辑提示**：若首页**总开关关闭**，卡片上的开启状态图标应呈现 **50% 透明度**，并提供 Tooltip 提示：“_全局通知已关闭，此提醒暂不生效_”。
    

#### **3. 逻辑判定漏斗 (Notification Funnel)**

只有通过以下所有关卡的卡片，才会最终触发浏览器通知：

| **层级**      | **判定项**                   | **若为 False 的结果**          |
| ----------- | ------------------------- | ------------------------- |
| **L1: 系统级** | 首页总铃铛是否为“填充色”？            | **全站静音**，不扫描到期卡片。         |
| **L2: 卡片级** | 当前卡片右上角开关是否开启？            | **该卡片跳过**，不触发推送。          |
| **L3: 时段级** | 是否处于设置中的 `Active Window`？ | **推迟发送**，直至进入活跃时段。        |
| **执行**      | 发送推送                      | 通过 `Service Worker` 弹出提醒。 |


### **## 需求补充：系统设置页“推送链路测试”功能**

#### **1. UI 交互设计**

- **位置**：放置在“系统设置”页面的 **“调度与通知设置”** 模块内。
    
- **组件形式**：一个醒目的按钮，文案为 **“发送测试通知 (Test Push)”**。
    
- **状态反馈**：
    
    - **Loading 态**：点击后按钮进入加载状态，防止用户重复点击。
        
    - **成功提示**：若后端成功调用推送服务器，显示 Toast 提示：“_测试请求已发送，请检查系统通知栏_”。
        
    - **错误拦截**：如果用户尚未开启 PWA 授权（未订阅），按钮应置灰，并在点击时弹出提醒：“_请先开启上方‘推送通知’开关以完成订阅_”。


### **## 需求：单卡“查看题目”与熟练度可视化看板**

**1. 入口与交互 (Drawer Entrance)**

- **菜单集成**：在卡片的 **Kebab Menu (三个点)** 中，原“数据洞察”替换为“**查看题目**”。
    
- **抽屉式布局**：点击后，MIMO 从页面右侧（或底部）滑出一个**抽屉式列表 (Drawer)**。这种方式不会中断用户在 Dashboard 的心流。
    

**2. 列表内容与视觉表现 (Visual List)**

- **题目展示**：列表清晰展示该卡片包含的所有题目（含 AI 生成与手动添加）。
    
- **熟练度标识 (The Level Bar)**：
    
    - **形态**：在每个题目后方紧跟一个长方形条。
        
    - **极简美学**：长方形条采用 0.5px 极细边框，保持与全站视觉语言一致。
        
    - **色系联动**：颜色深度完全跟随当前设置的**主题色**（如森林绿、复古粉、暮光紫等）。
        

**3. 熟练度渲染逻辑 (Color Depth Logic)**

熟练度由该题目在历史质询中被 AI 判定为 `passed` 的累计次数决定：

| **成功次数**      | **视觉表现 (基于主题色)**                  | **心理暗示**    |
| ------------- | --------------------------------- | ----------- |
| **0 次**       | **无填充 (Transparent)**             | 尚未攻克 / 全新题目 |
| **1 - 2 次**   | **低饱和度 / 浅色 (Lighter)**           | 初步接触        |
| **3 - 5 次**   | **中度饱和度 (Medium)**                | 产生记忆        |
| **6 次**       | **高饱和度 (Deep)**                   | 深度掌握        |
| **7 次 (Max)** | **纯正主题色 + 微微发光 (Primary & Glow)** | 形成本能 (结业)   |
## 需求：优化首页右上角刷新功能

用户点击刷新时，刷新按钮应该动态旋转