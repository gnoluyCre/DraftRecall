# DraftRecall 技术文档

一旦根目录架构、功能边界或技术选型变化，请更新本文档。

本文档说明 DraftRecall 当前实现的系统架构、核心数据流和各功能模块的技术原理。产品目标是把单篇草稿或文档转成可被反复质询的记忆卡片，通过费曼式问答、双轨调度和 7 轮复习，把短期草稿沉淀为可统计的知识仓库。

## 1. 技术栈

- 框架：Next.js 14 App Router，页面与 API 路由同仓库实现。
- 语言：TypeScript，领域模型集中定义在 `lib/types.ts`。
- UI：React 18、Tailwind CSS、少量 shadcn 风格基础组件、lucide-react 图标。
- 图表：Recharts，用于仓库 BI 的 Treemap、折线图和热力图。
- AI：OpenAI 兼容 Chat Completions 接口，用户在 `/settings` 配置 Provider、Base URL、API Key 和 Model ID。
- 持久化：浏览器 localStorage 为默认演示仓储，Supabase Postgres 作为生产表结构和服务端任务边界。
- PWA/通知：Web App Manifest、Service Worker、Web Push、VAPID。

## 2. 架构分层

DraftRecall 采用“页面组合体验、组件承载交互、lib 承载业务规则、API 封装外部能力”的分层。

- `app/`：Next.js 路由层。负责页面入口、布局、全局样式和服务端 API。
- `components/`：交互组件层。负责 Dashboard、录入弹窗、质询室、PWA 注册和仓库 BI。
- `lib/`：领域与基础设施边界。负责类型、调度算法、加密 AI 设置、OpenAI 兼容 AI 调用、状态仓储、推送发送和 Supabase 客户端。
- `public/`：PWA 静态资源。负责 manifest、图标和 Service Worker。
- `supabase/`：生产数据库结构。定义 Cards、Alarms、Records、Warehouse、Push Subscriptions 等表。

依赖方向保持单向：页面依赖组件，组件依赖 `lib`，API 路由依赖 `lib`，`lib` 不反向依赖页面或组件。这样核心业务规则可以在 UI 外被复用，也方便以后把 localStorage 替换为真正的 Supabase 数据仓储。

## 3. 核心领域模型

领域模型定义在 `lib/types.ts`。

- `Card`：复习卡片，是系统主实体。包含标题、原文、关键词、AI 生成的问题-要点组、卡片级解析/判分提示词、优先级、调度模式、通知开关、重置次数、当前阶段、已完成轮数、下一次复习时间和状态。
- `QuestionItem`：V1.2 的题目原子，包含 `question` 与动态 `keyPoints`，判分不再依赖整篇原文。
- `Alarm`：手动闹钟配置。包含时间、频率、启用状态和下一次触发时间。
- `ReviewRecord`：每次质询记录。保存问题、回答、反馈、结果、阶段变化和回答深度。
- `WarehouseEntry`：结业归档记录。保存已完成卡片的领域、关键词、轮数、重置次数、问题库、已掌握问题和结业报告。
- `AIProviderConfig`：全局 AI 引擎配置，包含 Provider Name、Base URL、API Key 和 Model ID。
- `DraftRecallState`：前端本地状态集合，由 `cards`、`records`、`warehouse`、`pushSubscriptions` 组成。

状态机以 `Card.status` 和复习阶段共同表达：

- `cooling`：尚未到下一次复习时间。
- `due`：已经到期，Dashboard 会将其放入待质询区域。
- `graduated`：完成 7 轮复习后从活跃卡片归档到仓库。
- `active`：类型上保留，但当前 UI 主要通过 `deriveCardStatus` 动态计算 `due/cooling`。

## 4. 数据流总览

### 4.1 新建卡片

1. 用户在 `CardComposer` 上传或粘贴单篇文档。
2. 组件从加密 localStorage 读取 AI 引擎配置，调用 `/api/ai/parse`，服务端路由转发给 `parseDocumentWithAI`。
3. OpenAI 兼容模型返回标题、关键词和 10 个费曼式问题，并为每个问题提取动态数量的 `keyPoints`；如果没有有效 AI 配置，则使用本地 fallback。
4. 组件根据用户选择创建 `Card`：
   - 曲线模式：调用 `computeCurveNextReview(0, now, activeWindow)` 计算首次复习时间。
   - 手动闹钟：调用 `computeManualNextFire` 创建每日闹钟，并仍保留曲线字段作为基础复习时间。
5. `upsertCard` 将卡片写入 localStorage。

### 4.2 Dashboard 展示

1. `app/page.tsx` 通过 `loadState` 读取本地状态。
2. `CardBoard` 调用 `deriveCardStatus` 判断每张卡片是否到期。
3. `sortCards` 按优先级、到期状态、下一次复习时间排序。
4. 看板展示 P0 核心资产、活跃 P1/P2 和可折叠冷却区。

### 4.3 质询与复习推进

1. 用户进入 `/inquiry/[cardId]`，页面渲染 `InquiryRoom`。
2. 当前问题由 `(stage + completedRounds) % questions.length` 选择，保证多问题轮换。
3. 每次会话默认随机抽取 3 个问题，用户可在问题面板中勾选单题或多题组合。
4. 每道题都有独立提交按钮。提交单题时，组件只向 `/api/ai/grade` 发送该题 `keyPoints`、回答、AI 配置和卡片判分提示词，不再发送全文。
5. 用户可反复“查看提示要点”，该操作只展开/收起 key points，不触发回退或失败。
6. `gradeAnswerWithAI` 基于 key points 评估回答，返回 0-100 分、反馈、命中要点和遗漏要点。
7. 当本次选择的题目都完成评分后，底部出现最终结算；只有每题最高得分均大于 60 分，该次质询才判定为通过；否则失败。
8. `applyReviewResult` 根据汇总结果推进或回退阶段：
   - `passed`：阶段加 1，完成轮数加 1。
   - `failed`：阶段减 1，完成轮数减 1，下限为 0。
   - `revealed`：用户直接看答案，阶段减 1，记录为未掌握。
9. 未结业时，卡片更新下一次曲线复习时间；结业时调用 `graduateCard` 生成仓库记录，并从活跃卡片移除。

### 4.6 卡片操作菜单

Dashboard 卡片右上角固定显示卡片级通知开关与 Kebab 菜单。菜单项包括：

- 设置：打开卡片深度设置抽屉。
- 重命名：即时修改标题并持久化。
- 设定等级：在 P0/P1/P2 间切换并触发看板重新排序。
- 查看题目：右侧抽屉展示全部题目和熟练度条，熟练度由历史 passed 次数映射到 0-7。
- 删除：二次确认后级联删除卡片及其复习记录。

### 4.7 通知漏斗

通知由三层开关共同决定：

- L1 系统级：Dashboard 顶栏铃铛控制本地 `notificationSettings.globalEnabled`；生产定时任务也会读取 Supabase `notification_settings.global_enabled`。
- L2 卡片级：每张卡片右上角铃铛控制 `Card.notificationsEnabled`。
- L3 时段级：服务端到期扫描检查卡片 `activeWindow`，不在时段内则延迟。

### 4.4 仓库 BI

1. `/warehouse` 页面读取 `warehouse` 和 `records`。
2. `WarehouseBI` 将归档卡片按 `domain` 聚合为知识分布 Treemap。
3. 按 `graduatedAt` 聚合为掌握速率折线。
4. 按 `records.createdAt` 和 `depth` 聚合最近 35 天质询热力。
5. 聚合活跃卡片与仓库卡片的 `resetCount`，展示高频重置的认知瓶颈。
6. 仓库卡片展示问题掌握覆盖率，用已超过 60 分的问题映射到问题库分布。

### 4.5 PWA 与推送

1. `PwaRegister` 注册 `/sw.js`，并在用户授权后创建 Push Subscription。
2. `/api/push/subscribe` 将订阅写入 Supabase；没有 Supabase 时落入进程内数组，仅适合本地演示。
3. `/api/alarms/due` 供定时任务调用，扫描 Supabase 中到期且未结业的卡片。
4. 到期卡片通过 `sendPush` 发送 Web Push，随后把卡片状态更新为 `due`。
5. Service Worker 接收 push 事件展示通知，点击通知后聚焦或打开对应质询页。

## 5. 调度算法原理

调度核心位于 `lib/scheduler.ts`。

### 5.1 费曼曲线

`CURVE_INTERVALS_MINUTES` 当前为：

```ts
[30, 720, 1440, 2880, 5760, 10080, 21600]
```

对应 30 分钟、12 小时、1 天、2 天、4 天、7 天、15 天。`computeCurveNextReview` 根据当前阶段选择间隔，并调用 `clampToActiveWindow` 把时间限制在用户设置的活跃时段内。

### 5.2 活跃时段约束

`clampToActiveWindow` 支持两种窗口：

- 普通窗口，例如 08:00-22:00：早于开始则推到开始，晚于结束则推到次日开始。
- 跨日窗口，例如 22:00-02:00：只要目标时间落在夜间窗口内就保留，否则推到窗口开始。

### 5.3 手动闹钟

`computeManualNextFire` 以指定时刻为基础计算下一次触发：

- `once` 和 `daily`：如果今天已过该时刻，则顺延到明天。
- `weekdays`：跳过周末。
- `custom`：使用传入的星期数组；没有传入时默认使用当前星期。

`getEffectiveNextReview` 会优先选择启用的手动闹钟中最早的 `nextFireAt`，否则使用曲线 `nextReviewAt`。

### 5.4 通过、失败与结业

`applyReviewResult` 是复习状态推进的唯一核心规则：

- 通过：`stage + 1`，`completedRounds + 1`。
- 失败：`stage - 1`，`completedRounds - 1`。
- 直接看答案：按未掌握处理，`stage - 1`。
- 阶段范围被限制在 0-7。
- 当通过后 `completedRounds >= 7`，卡片结业。

结业由 `graduateCard` 生成 `WarehouseEntry`，其 `domain` 目前取第一个关键词，报告由通过次数、直接看答案次数和轮数拼接生成。

`resetMemoryCurve` 提供危险操作“重置记忆曲线”：它把 `stage` 和 `completedRounds` 归零，把 `nextReviewAt` 强制设为当前时间 + 30 分钟，不套用活跃时段，并将 `resetCount` 加 1。

## 6. AI 集成原理

AI 边界位于 `lib/ai-service.ts`，设置持久化位于 `lib/ai-settings.ts`，API 路由只负责参数校验和 JSON 响应。

- 全局设置：`/settings` 收集 Provider Name、Base URL、API Key、Model ID，并用 AES-GCM 加密保存到 localStorage；API Key 重新展示时只显示脱敏文本。
- 连接测试：`/api/ai/test` 使用当前配置调用 Chat Completions，返回可用状态和延迟。
- 文档解析：默认 Master Prompt 要求只返回 JSON，结构为 `title`、`keywords`、`questionItems`，并生成 10 个初始问题和动态 key points。
- 回答评估：默认 Master Prompt 要求只返回 JSON，结构为 `passed`、`score`、`feedback`、`gaps`、`hitPoints`、`missedPoints`，其中 `score` 是 0-100。
- 卡片级提示词：如果卡片没有自定义解析或判分提示词，严格回退使用系统默认提示词。
- 输入截断：文档内容发送给模型前限制到 18000 字符，避免超出上下文和控制成本。
- JSON 容错：`safeJson` 会去掉 Markdown 代码块标记，并在解析失败时返回 fallback。
- 降级策略：没有 AI 配置时，系统仍能用本地规则完成录入和质询演示；配置错误或网络超时时，API 返回清晰错误。

当前实现直接读取 `.txt/.md/.pdf` 文件文本；浏览器 `file.text()` 不能解析真实 PDF 二进制内容，因此 PDF 若需要生产可用，应后续接入 PDF 文本抽取。

## 7. 持久化设计

### 7.1 本地仓储

`lib/storage.ts` 使用 `draftrecall.state.v1` 作为 localStorage key。

- `loadState`：读取 JSON，失败时恢复 seed 数据。
- `saveState`：整体写回状态。
- `upsertCard`：新增或覆盖卡片。
- `addRecord`：追加质询记录。
- `archiveCard`：从活跃卡片移除并写入仓库。
- V1.1 迁移：为旧卡片补齐 `parsePrompt`、`gradePrompt`、`resetCount`，并把问题库限制到 20 个。
- V1.2 迁移：为旧卡片补齐 `questionItems`、`notificationsEnabled`、`notificationSettings` 和 `theme`。

这种实现适合离线演示和单设备使用，写入粒度是整个状态对象，因此多人协作、跨设备同步和冲突解决应交给 Supabase 仓储实现。

### 7.2 Supabase 生产表

`supabase/schema.sql` 定义生产数据结构：

- `cards`：卡片主表，含优先级、调度模式、阶段、下一次复习时间和状态。
- `ai_settings`：OpenAI 兼容 AI 引擎配置表，生产环境可改为服务端加密后存储。
- `notification_settings`：生产定时任务使用的全局通知开关表。
- `alarms`：手动闹钟表，与卡片级联删除。
- `records`：质询记录表。
- `warehouse`：结业仓库表。
- `push_subscriptions`：浏览器推送订阅表。

索引围绕主查询路径设计：

- `cards(priority, status, next_review_at)` 支持看板和到期扫描。
- `alarms(enabled, next_fire_at)` 支持闹钟扫描。
- `records(card_id, created_at desc)` 支持卡片历史。
- `warehouse(domain, graduated_at desc)` 支持 BI 聚合。

## 8. API 路由

- `POST /api/ai/parse`：输入 `content`、`filename`、AI 配置和可选解析提示词，输出解析后的标题、关键词、问题。
- `POST /api/ai/grade`：输入 `question`、`keyPoints`、`answer`、AI 配置和可选判分提示词，输出 AI 评分与反馈。
- `POST /api/ai/test`：输入 AI 配置，输出连接状态和延迟。
- `POST /api/push/subscribe`：接收浏览器 Push Subscription，优先写入 Supabase。
- `POST /api/push/send`：输入单个 subscription 和 payload，直接发送 Web Push。
- `POST /api/alarms/due`：定时扫描到期卡片并推送通知，需要 Supabase service role 环境变量。

API 路由不持有复杂业务规则；规则下沉到 `lib`，保证 UI 和服务端任务可以复用同一套逻辑。

## 9. PWA 离线与通知

`public/sw.js` 实现三个能力：

- 安装阶段缓存 `/`、`/manifest.webmanifest`、`/icon.svg`。
- GET 请求网络失败时回退到缓存资源或首页。
- 收到 Push 后展示 requireInteraction 通知，点击后打开 payload 指定的 URL。

`public/manifest.webmanifest` 负责安装信息，`components/pwa-register.tsx` 负责在浏览器端注册 Service Worker 和申请通知权限。

## 10. 环境变量

`.env.example` 给出所有可选和必需的部署变量：

- `NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_ANON_KEY`：客户端 Supabase。
- `SUPABASE_SERVICE_ROLE_KEY`：服务端到期扫描和订阅写入。
- `NEXT_PUBLIC_VAPID_PUBLIC_KEY`、`VAPID_PRIVATE_KEY`、`VAPID_SUBJECT`：Web Push。

缺少 Supabase 时，主应用仍可用 localStorage 运行；缺少用户 AI 配置时，AI 能力降级为本地 fallback；缺少 VAPID 时，推送发送会抛出 `Missing VAPID keys`。

## 11. 当前实现边界

- localStorage 是默认运行路径，Supabase schema 和部分 API 已准备好，但前端仓储尚未完整切到远端；AI 设置当前采用加密 localStorage，`ai_settings` 表为生产扩展边界。
- 推送订阅 API 的内存数组只适合本地调试，生产应依赖 Supabase。
- PDF 上传当前按文本读取，尚未做真实 PDF 解析。
- `/api/alarms/due` 当前扫描 `cards.next_review_at`，没有单独扫描 `alarms.next_fire_at`。
- 没有用户认证和行级安全策略，`user_id` 字段为后续多用户扩展预留。

## 12. 开发与文档维护规则

- 修改 `app/` 页面或 API 时，同步更新 `app/README.md` 和本文档中的相关数据流。
- 修改 `components/` 交互组件时，同步更新 `components/README.md` 和对应功能原理。
- 修改 `lib/` 领域规则、AI、存储、推送时，同步更新 `lib/README.md`、文件头三行注释和本文档。
- 修改 `supabase/schema.sql` 时，同步更新 `supabase/README.md` 和本文档的持久化章节。
- 新增功能时先确认它属于页面、组件、领域服务、API、PWA 还是数据库边界，避免把业务规则散落在 UI 事件里。
